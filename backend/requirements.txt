# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# ---- Validation / settings ----
pydantic>=2.7,<3.0
pydantic-settings>=2.3

# ---- Numerical / signal processing ----
# scipy-only: keeps the deploy bundle small enough for serverless hosts
# (Vercel caps Python functions at 500 MB). The frontend uploads 16-bit WAV,
# which scipy decodes directly; STFT/pitch-shift use scipy.signal equivalents.
numpy>=1.26,<1.27
scipy>=1.13,<1.15

# ---- Computer vision ----
# headless: no GUI/Qt dependencies, safe on servers
opencv-python-headless>=4.9,<4.11
pillow>=10

# ---- AI (optional: Gemini-backed processors) ----
# Enable with PERCEPTA_USE_AI_PROCESSORS=true + PERCEPTA_GEMINI_API_KEY.
# The pipeline degrades to rule-based stages automatically if either is missing.
google-genai>=2.0

# ---- Tests ----
pytest>=8
httpx>=0.27
